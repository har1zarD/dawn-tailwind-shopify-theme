<!-- Welcome section starts -->
<section class="metafields-demo">
  <div class="customize-section">
    <div class="section-inner">
      {% if section.settings.section-heading != blank %}
      <div class="label">
        {{ section.settings.section-heading }}
      </div>
      {% endif %} {% if section.settings.section-large-heading != blank %}
      <div class="section-right">
        <div class="metafield input">
          <p>{{ section.settings.section-large-heading }}</p>
        </div>
        {% endif %} {% if section.settings.description != blank %}
        <div class="metafield">
          {{ section.settings.description }}
        </div>
      </div>
   {% if section.settings.checkbox %}
  <div class="checkbox-container" style="background-color: green;">
    <!-- Ovdje možeš dodati dodatni HTML ili tekst ako je checkbox true -->
  </div>
{% else %}
  <div class="checkbox-container" style="background-color: red;">
    <!-- Ovdje možeš dodati dodatni HTML ili tekst ako je checkbox false -->
  </div>
{% endif %}

          
      {% endif %}
    </div>
  </div>
</section>
<!-- Welcome section ends -->

{% schema %}
{
  "name": "Metafields Test", // Section name
  "settings": [
    { "id": "section-heading", "type": "text", "label": "Metafield name" },
    { "id": "section-large-heading", "type": "textarea",  "label": "Metafield" } ,
    { "id": "description", "type": "liquid", "label": "Output" },
    { "id": "checkbox", "type": "checkbox", "label": "Works?" }
  ],
  "presets": [
    { "name": "Accentuate Test", "category": "ADVANCED LAYOUT" }
  ]
}
{% endschema %}

{% stylesheet %}
<style>
  
{
  margin-bottom: 20px;
}
  
  .checkbox-container{
    display:flex;
    padding: 10px;
  }
  
  .metafields-demo {
    display: flex;
    flex-wrap: wrap;  
  }

  .customize-section {
    width: 100%;
    max-width: 1120px;
    margin: auto;
  }

  .label {
    min-width: 180px;
  }

  .section-inner {
    display: flex;
    border-radius: 5px;
    column-gap: 20px;
    background-color: #475569;
    padding: 14px;
    color: white;
    align-items: center;
  }

  .section-inner + .section-inner {
    margin-top: 6px;
  }

  .section-right {
    display: grid;
    gap: 10px;
    grid-template-columns: 1fr 1fr;
    width: 100%;
  }

  .metafield {
    color: #1e293b;
    padding: 5px 15px;
    border-radius: 5px;
    background-color: white;
    overflow-wrap: anywhere;
  }

  .metafield.input {
    font-family: monospace;
    background-color: black;
    color: white;
  }
</style>
{% endstylesheet %}

{% javascript %}
{% endjavascript %}
